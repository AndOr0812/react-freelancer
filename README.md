A ReactJS implementation of Freelancer theme from StartBootstrap (https://startbootstrap.com/themes/freelancer/).

This project is created with [create-react-app](https://github.com/facebook/create-react-app).

To run:
```
npm start
```

To build:
```
npm build
```
